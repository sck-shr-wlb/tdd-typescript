# Stub

1. start mountebank
    ```shell
    mb start --configfile iWorkflow.json
    ```

2. install newman
    ```shell
    sudo npm install newman -g   
    ```

3. run newman 
    ```shell
    newman run [file_collection_postman]  
    ```